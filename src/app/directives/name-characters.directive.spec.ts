import { NameCharactersDirective } from './name-characters.directive';

describe('NameCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new NameCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
